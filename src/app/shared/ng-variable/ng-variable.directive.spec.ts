import { NgVariableDirective } from './ng-variable.directive';

describe('NgVariableDirective', () => {
    it('should create an instance', () => {
        const directive = new NgVariableDirective();
        expect(directive).toBeTruthy();
    });
});
